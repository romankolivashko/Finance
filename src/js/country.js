export default class Destination {
  constructor(name, region, gdp, population) {
    // this.DestinationId = destinationId;
    this.Name = name;
    this.Region = region;
    this.Gdp = gdp;
    this.Population = population;
  }
}