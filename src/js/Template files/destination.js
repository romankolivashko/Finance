export default class Destination {
  constructor(username, country, city, rating, review) {
    // this.DestinationId = destinationId;
    this.Username = username;
    this.Country = country;
    this.City = city;
    this.Rating = rating;
    this.Review = review;
  }
}